fos_user:
    db_driver: orm # other valid values are 'mongodb' and 'couchdb'
    firewall_name: main
    user_class: App\Entity\User
    service:
        mailer: 'fos_user.mailer.noop'
    from_email:
        address: 'no-replay@gmail.com'
        sender_name: 'no-replay'
